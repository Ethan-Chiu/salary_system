import { Char } from "./utils";

export abstract class BaseMeta {
	// @CreateDateColumn({ type: "date" })
	// create_date: Date;

	// @Column("varchar2", { length: Char(32), default: "admin" })
	// create_by: string;

	// @UpdateDateColumn({ type: "date" })
	// update_date: Date;

	// @Column("varchar2", { length: Char(32), default: "admin" })
	// update_by: string;
}
